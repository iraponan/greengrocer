mixin StorageKeys {
  static const String token = 'token';
}

mixin ConfigPage {
  static const int itemsPerPage = 6;
}

mixin PaymentsConfig {
  static const Duration daysValid = Duration(days: 7);
}
